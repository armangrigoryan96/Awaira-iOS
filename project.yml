name: Awaira
options:
  bundleIdPrefix: com.awaira
  deploymentTarget:
    iOS: "18.0"
  createIntermediateGroups: true

targets:
  Awaira:
    type: application
    platform: iOS
    sources:
      - Sources
    info:
      path: Sources/Info.plist
      properties:
        CFBundleName: Awaira
        CFBundleDisplayName: Awaira
        CFBundleShortVersionString: "1.3"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        ITSAppUsesNonExemptEncryption: false
        UILaunchScreen: {}
        UIRequiresFullScreen: true
        # Detection geometry assumes a portrait, upright buffer — see Detector.configureAndRun().
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSCameraUsageDescription: "Awaira uses the camera to notice when your hand reaches your face. Video is processed entirely on-device and never leaves your iPhone."
        # These must match the subscription products and non-consumable Lifetime Unlock configured
        # in App Store Connect for com.awaira.ios. StoreKit retrieves localised prices from them.
        AwairaMonthlyProductID: com.awaira.ios.sub.monthly
        AwairaYearlyProductID: com.awaira.ios.sub.yearly
        AwairaLifetimeProductID: com.awaira.app.lifetime
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.awaira.ios
        # Sources/Assets.xcassets — the app icon, taken from the Mac app so the two look like one
        # product (Awaira-Main: frontend/Sources/Assets.xcassets).
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        MARKETING_VERSION: "1.3"
        CURRENT_PROJECT_VERSION: "12"
        GENERATE_INFOPLIST_FILE: NO
        SWIFT_VERSION: "5.0"
        TARGETED_DEVICE_FAMILY: "1"
        DEVELOPMENT_TEAM: S6HG6FS5JJ
        CODE_SIGN_STYLE: Automatic

  AwairaTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests/AwairaTests
    dependencies:
      - target: Awaira
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.awaira.ios.tests
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "5.0"

  AwairaUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - Tests/AwairaUITests
    dependencies:
      - target: Awaira
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.awaira.ios.uitests
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "5.0"

schemes:
  Awaira:
    build:
      targets:
        Awaira: all
        AwairaTests: [test]
        AwairaUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      gatherCoverageData: true
      targets:
        - AwairaTests
        - AwairaUITests
